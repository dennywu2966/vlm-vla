[project]
name = "vlm-vla"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1.0",
    "lerobot[smolvla,peft,libero]",
    "huggingface_hub",
    "wandb",
]

[project.optional-dependencies]
openvla = [
    "bitsandbytes>=0.43.0",
    "accelerate>=0.26.0",
]
dev = [
    "pytest",
    "ruff",
]
